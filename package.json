{
  "name": "groam-workspace",
  "version": "0.0.0",
  "dependencies": {
    "@groam/env": "workspace:*",
    "convex": "^1.45.0",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@convex-dev/static-hosting": "0.2.1",
    "@types/node": "^26.4.1",
    "fallow": "3.22.0",
    "husky": "^9.1.7",
    "jiti": "^2.7.0",
    "lint-staged": "^17.5.0",
    "react-doctor": "0.9.13",
    "syncpack": "^15.3.3",
    "turbo": "^2.10.12",
    "typescript": "~7.0.2",
    "vite": "^8.2.2"
  },
  "license": "MIT",
  "lint-staged": {
    "*": "biome check --write --unsafe --no-errors-on-unmatched"
  },
  "overrides": {
    "fast-uri": "^3.1.6",
    "hono": "^4.13.5",
    "js-yaml": "^4.3.2",
    "nanoid": "^3.3.17",
    "qs": "^6.16.0",
    "undici": "^7.29.0",
    "zod": "^4.6.2"
  },
  "packageManager": "bun@1.3.11",
  "private": true,
  "scripts": {
    "audit": "bun audit",
    "auth:schema": "bun run --cwd packages/backend auth:schema",
    "build": "turbo run build",
    "build:web": "STATIC_HOSTING_BASE_PATH=/ VITE_GROAM_SHELL=web bun run --cwd apps/web build",
    "build:web:desktop": "STATIC_HOSTING_BASE_PATH=/ VITE_GROAM_SHELL=desktop VITE_CONVEX_URL=http://127.0.0.1:3210 VITE_SITE_URL=http://127.0.0.1:3211/ bun run --cwd apps/web build",
    "check": "bun run audit && bun run typecheck && bun run lint && bun run test && bun run react-doctor && bun run fallow",
    "codegen": "bun run codegen:routes && bun run codegen:auth && bun run codegen:convex",
    "codegen:auth": "bun run auth:schema",
    "codegen:convex": "convex codegen",
    "codegen:routes": "bun run --cwd apps/web routes:generate",
    "convex:dashboard": "convex dashboard",
    "convex:deploy": "convex deploy --typecheck-components",
    "convex:dev": "convex dev --typecheck-components",
    "deploy": "bun run convex:deploy && bun run migrations:run:prod && bun run hosting:upload:web:prod",
    "dev": "turbo run dev --ui=tui --filter=!@groam/desktop",
    "dev:desktop": "bun run --cwd apps/desktop dev",
    "dev:web": "bun run --cwd apps/web dev",
    "fallow": "bunx fallow -c tooling/fallow.config.jsonc --fail-on-issues && bunx fallow -c tooling/fallow.config.jsonc health --css",
    "fallow:changed": "bunx fallow audit -c tooling/fallow.config.jsonc --changed-since HEAD --gate all && bunx fallow security -c tooling/fallow.config.jsonc --changed-since HEAD --gate new",
    "fallow:strict": "bunx fallow -c tooling/fallow.config.jsonc --fail-on-issues --type-aware-require complete && bunx fallow -c tooling/fallow.config.jsonc health --css --type-aware-require complete",
    "format": "syncpack format --config tooling/syncpack.config.json && biome check --write --unsafe .",
    "format:check": "syncpack format --check --config tooling/syncpack.config.json && biome format .",
    "hosting:upload:web": "static-hosting upload --component web --dist apps/web/dist --build-command 'bun run build:web'",
    "hosting:upload:web:desktop": "static-hosting upload --component web --dist apps/web/dist --build-command 'bun run build:web:desktop'",
    "hosting:upload:web:prod": "static-hosting upload --prod --component web --dist apps/web/dist --build-command 'bun run build:web'",
    "lint": "bun run lint:conventions && bun run lint:packages && biome check .",
    "lint:conventions": "bun tooling/quality/check-conventions.ts",
    "lint:packages": "syncpack lint --config tooling/syncpack.config.json",
    "migrations:run": "convex run migrations:runAll '{}'",
    "migrations:run:prod": "convex run migrations:runAll '{}' --prod",
    "migrations:status": "convex run --component migrations lib:getStatus",
    "preconvex:dev": "bun tooling/devkit/prepare-convex.ts",
    "predev": "bun tooling/devkit/prepare-convex.ts",
    "prepare": "husky tooling/husky",
    "preview": "turbo run preview --parallel",
    "react-doctor": "react-doctor -y --blocking warning --no-dead-code --no-score .",
    "react-doctor:changed": "react-doctor -y --blocking warning --scope changed --base origin/main --no-dead-code --no-score .",
    "react-doctor:staged": "react-doctor --blocking warning --staged --no-dead-code --no-score .",
    "seed": "bun --env-file .env.local tooling/seeder/src/cli.ts",
    "showcase": "bun run seed -- --scale realistic && bun run --cwd tooling/showcase showcase",
    "showcase:capture": "bun run --cwd tooling/showcase capture",
    "showcase:edit": "bun run --cwd tooling/showcase edit",
    "showcase:render": "bun run --cwd tooling/showcase render",
    "showcase:setup": "bun run --cwd tooling/showcase setup",
    "showcase:studio": "bun run --cwd tooling/showcase studio",
    "test": "bun test --path-ignore-patterns apps/desktop/src-tauri tooling/quality && turbo run test",
    "test:e2e": "bun run --cwd apps/web e2e",
    "test:e2e:install": "bun run --cwd apps/web e2e:install",
    "test:e2e:local": "bun run --cwd apps/web e2e:local",
    "test:e2e:local:headed": "bun run --cwd apps/web e2e:local:headed",
    "typecheck": "turbo run typecheck && tsc --noEmit -p tooling/tsconfig.json"
  },
  "trustedDependencies": [
    "fallow"
  ],
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/app-actions",
    "tooling/devkit",
    "tooling/seeder",
    "tooling/showcase",
    "tooling/typescript-config",
    "tooling/vitest-config"
  ]
}
