# @groam/ai-contracts

Backend- and browser-neutral assistant contracts: agent definitions, run and screen DTOs,
provider settings, error presentation, and the structured-output catalog. Runtime tools and
provider adapters live in `@groam/backend`; React hooks and assistant UI live in `@groam/ui/ai`.
