# Output

Structured UI catalog for `@json-render` specs the assistant emits (headings, inputs, recap cards, etc.).

This is **not** chat UI — it is the schema and component registry the model targets when building interactive forms.

Import via `@groam/ai-contracts/output`. Registry also re-exports component IDs for prompt instructions.
